services:
  - type: web
    name: pastbackbonegztxx7
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: API_TOKEN
        sync: false
      - key: PORT
        value: 10000
    healthCheckPath: /health
